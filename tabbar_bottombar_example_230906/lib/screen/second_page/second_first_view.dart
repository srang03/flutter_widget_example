import 'package:flutter/material.dart';

class SecondFirstView extends StatelessWidget {
  const SecondFirstView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('SecondFirstView'),
    );
  }
}
