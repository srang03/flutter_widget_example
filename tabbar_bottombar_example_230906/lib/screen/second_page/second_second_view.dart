import 'package:flutter/material.dart';

class SecondSecondView extends StatelessWidget {
  const SecondSecondView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('SecondSecondView'),
    );
  }
}
